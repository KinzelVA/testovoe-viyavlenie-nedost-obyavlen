# testovoe-viyavlenie-nedost-obyavlen
# Avito Gap Analyzer

Инструмент для выявления недостающих товарных позиций на Avito. 
Автоматизирует процесс: Парсинг → Обогащение данных (API) → Анализ покрытия (Plan vs Fact).

## 🚀 Функционал
1. **Парсинг Avito**: Сбор объявлений по заданному поисковому запросу с обходом базовых защит.
2. **Обогащение**: Нормализация заголовков через внешний сервис (top505.ru) для получения марок, моделей и категорий.
3. **Аналитика**: Сравнение текущей выдачи Avito с эталонным каталогом номенклатуры.

## 🛠 Установка и запуск

**Требования:**
- Python 3.8+
- Установленные браузеры Playwright

**1. Установка зависимостей:**
```bash
pip install -r requirements.txt
playwright install chromium
2. Конфигурация: Основные настройки находятся в src/config.py:

AVITO_SEARCH_QUERY — поисковый запрос (по умолчанию "Натяжитель гусеницы")

TARGET_ADS_COUNT — количество объявлений для парсинга

3. Запуск пайплайна:

Bash

python main.py
Скрипт последовательно выполнит все шаги и сохранит результаты в папку data/.

🏗 Архитектура
1. Парсер (src/avito_parser.py)
Использует Playwright для эмуляции реального пользователя.

Особенности:

headless=False: Браузер открывается визуально (снижает риск бана).

Random delays: Случайные задержки между действиями.

Smart Selectors: Трехуровневый поиск заголовка (itemprop, h3, title), чтобы избежать пустых данных.

2. API Обогащение (src/api_enricher.py)
Интеграция с сервисом top505.ru.

Метод: Батчевая обработка (по 5 товаров).

Обработка ошибок:

429 Too Many Requests: Пауза 5 сек и повтор.

5xx Server Error: Экспоненциальная задержка и 3 попытки ретрая.

Сохранение исходных данных даже при сбое API.

3. Анализатор (src/analyzer.py)
Сравнивает "План" (output.csv) и "Факт" (ads_enriched.json).

Ключ сравнения: Комбинация group0 + group1 + group2 + marka + model.

Нормализация: Приведение строк к нижнему регистру, очистка от пробелов.

📊 Результаты (Артефакты)
Все файлы сохраняются в папку data/:

ads_raw.json — "Сырые" данные с Avito.

ads_enriched.json — Данные после обработки нейросетью/API.

missing_coverage.csv — Итоговый отчет: список позиций, которых нет в продаже.

🔜 План развития (Roadmap на 2 недели)
Если бы у меня было больше времени, я бы внедрил:

Масштабирование парсинга:

Подключение ротации Proxy (мобильные прокси).

Использование stealth плагинов для Playwright, чтобы скрыть автоматизацию.

Асинхронный режим (asyncio) для ускорения работы.

Улучшение аналитики:

Нечеткое сравнение строк (Fuzzy Matching) для марок и моделей (чтобы Cat и Caterpillar считались одним брендом).

Дашборд на Streamlit для визуализации "дыр" в покрытии.

DevOps:

Упаковка в Docker контейнер.

Настройка CI/CD для ежедневного запуска по расписанию.
